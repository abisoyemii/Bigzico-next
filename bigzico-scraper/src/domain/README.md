# Domain

This folder contains the core domain interfaces and shared business contracts used by all services.

It defines the abstractions for scrapers, importers, plugins, queues, logging, engines, image pipeline, and error handling.
