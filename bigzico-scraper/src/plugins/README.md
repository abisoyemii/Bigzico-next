# Plugins

This module defines plugin architecture and manifest loading for store-specific scrapers and engine adapters.

Plugin manifests are validated against a schema and loaded from the `src/registry/manifests` folder.
