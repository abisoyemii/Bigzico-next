# Queue

This module contains the queue abstraction used by worker and scheduler components.

It defines the `IQueue` contract and will be implemented by BullMQ or another queue provider in later phases.
