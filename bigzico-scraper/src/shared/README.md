# Shared Layer

The Shared layer contains cross-cutting platform concerns.

Responsibilities:
- Configuration, constants, environment validation, and typed errors.
- Logging, diagnostics, event names, defaults, and operational utilities.
- Runtime metadata used by all bounded contexts.

This layer centralizes common concerns to avoid duplication.
