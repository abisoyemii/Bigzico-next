import { logger } from '../common/logger';

export function startImportService() {
  logger.info('Import service placeholder started');
  // Accepts normalized payloads and will validate/transform upserts in Phase 3
}
