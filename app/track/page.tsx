import { InfoPage } from '@/components/content/InfoPage';
export default function TrackPage() { return <InfoPage eyebrow="Delivery" title="Track Your Order" description="Enter your order reference to follow its delivery progress." points={['Order received', 'Preparing for dispatch', 'Out for delivery', 'Delivered safely']} />; }
